# cronjob
# Запускать скрипт каждые 5 минут.
# Пользователь: root (стандартный пользователь в контейнере)
# Команда: абсолютный путь к python и к скрипту
# Перенаправление вывода: stdout (>>) и stderr (2>>) в стандартный вывод процесса Docker (PID 1)
*/5 * * * * root /usr/local/bin/python /app/main.py >> /proc/1/fd/1 2>>/proc/1/fd/2

# ВАЖНО: cron требует пустую строку в конце файла для корректной работы.

