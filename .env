# This file contains non-secret environment variables for the application.
# It is committed to version control.
# Secret values are injected during the GitHub Actions workflow from GitHub Secrets.

# -- GitHub Secrets --
# The following variables must be set as GitHub Secrets in your repository settings:
# DROPBOX_APP_KEY
# DROPBOX_APP_SECRET
# OPENAI_API_KEY
# DOCKER_USERNAME (for Docker Hub)
# DOCKER_HUB_ACCESS_TOKEN (for Docker Hub)

# -- Core Application Settings --
# These settings are safe to commit and can be configured here.
LOG_LEVEL="INFO"
DROPBOX_SOURCE_DIR=""
DROPBOX_DEST_DIR="/rm2"
DROPBOX_FAILED_DIR="/failed_files"
RECOGNITION_MODEL="gemini-2.5-flash"
RECOGNITION_PROMPT="Recognize the handwritten text in the image."
PDF_DPI=200
LOOP_SLEEP_SECONDS=120

# -- Docker Image Tag --
# This is set automatically by the CI/CD pipeline.
REMREC_IMAGE_TAG="latest"

# -- Workflow Settings (Optional) --
# Maximum time in seconds to wait for the file lock.
# LOCK_TIMEOUT=5
