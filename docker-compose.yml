# docker-compose.yml
version: '3.8'

services:
  app:
    # The name of the container that will be created
    container_name: remarkable-app
    
    # The name of the image that will be built
    image: remarkable-recognizer
    
    # Tell Docker Compose to build the image from the Dockerfile
    # in the current directory ('.')
    build: .
    
    # Specify the file with environment variables (secrets)
    env_file:
      - .env
      
    # Restart policy. 'unless-stopped' means the container
    # will automatically restart (e.g., after a server reboot)
    # unless it was manually stopped.
    restart: unless-stopped
